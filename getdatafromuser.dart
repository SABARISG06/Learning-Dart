import 'dart:io';

void main() {
  var name;
  // int age;
  // int month;
  print("Enter your name age month:");
  name = stdin.readLineSync();
  // age = int.parse(stdin.readLineSync());
  // month = int.parse(stdin.readLineSync());
  print(name);
  // print(age);
  // print(month);
}
